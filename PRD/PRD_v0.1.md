# Float Dock PRD v2.2

## 🎯 核心需求 (Core Requirements)
- **轻量高性能启动器:** 基于 Rust + egui，单文件部署，内存占用目标 < 30MB，冷启动目标 < 500ms。
- **全局三热键:**  
  `Ctrl+Alt+Shift+[` 唤起并聚焦窗口  
  `Ctrl+Alt+Shift+]` 隐藏窗口（进程继续驻留，可再次唤起）  
  `Ctrl+Alt+Shift+\` 终止程序（完全退出）。
- **应用固定与启动:** 支持拖拽 `.exe` / `.lnk` / 文件夹固定，单击选中，双击启动。  
  拖入 `.lnk` 时自动解析并保存为目标路径（等价直接固定目标）。
- **图标缓存机制:** 图标提取后写入本地缓存，重启恢复；支持图标尺寸 16-128px。  
  `.lnk` 优先解析目标程序图标并缓存，避免仅显示快捷方式白纸图标。
  支持 `ico` 目录自定义图标覆盖（`应用名.ico` 优先，`favicon.ico` 可兜底）。
- **品牌图标一致性:** `ico/app.ico` 作为构建期资源注入 `exe` 图标，并在运行时用于系统托盘图标（隐藏/唤起状态一致）。
- **窗口体验:** 无边框、可拖拽、可边缘拉伸缩放、位置与尺寸记忆、九宫格吸附，并限制标题栏不移出屏幕，符合 Windows 11 视觉风格。
- **应用栏排序:** 长按约 260ms + 18px 阈值进入拖拽排序，落位后立即持久化。

## 🖼️ 界面描述 (Interface Description)
- **主窗口形态:** 半透明浮层 + 顶部信息栏 + 可折叠应用列表。
- **风格方向:** 深色玻璃质感、柔和阴影、圆角边框、轻量动画反馈。
- **顶部信息栏:** 仅保留 `Float Dock` 单行标题，不显示副文案与快捷键提示。
- **列表条目:**  
  高度约 46dp，图标 20x20，增强可读性对比度，仅显示应用名，支持 hover/选中态。  
  右键提供删除操作，点击 `Remove` 立即移除（无二次确认）。
- **右键菜单（主面板）:** 保留 `Auto-start` / `Two-column mode` / `Quit` 三项，字体黑色，去除历史遗留配置项。
- **空状态:** 无条目时显示拖拽引导文案与高亮投放区域。
- **拖拽反馈:** 排序中显示占位槽和悬浮条目，落位使用约 200ms 弹性过渡。

## ⚙️ 技术约束 (Technical Constraints)
- **平台:** Windows 10 / Windows 11。
- **技术栈:** Rust 2021、eframe/egui、windows-rs（含原生 `RegisterHotKey`）、tray-icon。
- **性能指标:**
  - 内存: 目标 < 30MB（上限约束 < 50MB）
  - 空闲 CPU: < 1%
  - 输入响应: < 100ms
- **兼容约束:**  
  拖拽固定仅接受 `.exe/.lnk/文件夹`；重复固定需拦截并提示。  
  隐藏状态不得销毁热键监听与托盘能力。  
  默认加载 Windows CJK 字体回退（如微软雅黑）以保证中文标题与提示可见。

## 🧱 代码架构重构 (Refactor Summary)
为降低 `app.rs` 单文件复杂度，已拆分为模块化结构：

| 模块 | 责任 |
| :--- | :--- |
| `src/app.rs` | 应用组装、核心状态、通用状态方法 |
| `src/app/runtime.rs` | 托盘、全局热键、事件循环、图标后台线程 |
| `src/app/state.rs` | `PinnedApp` / `DropAnim` 等状态模型 |
| `src/app/style.rs` | 主题色、尺寸与样式常量 |
| `src/app/ui.rs` | UI 渲染、交互逻辑、动画与弹窗流程 |

## 🔄 本次迭代重点 (Current Iteration Focus)
- 热键主组合切换为低冲突按键：`Ctrl+Alt+Shift+[` / `Ctrl+Alt+Shift+]` / `Ctrl+Alt+Shift+\`。
- 热键保留兼容兜底组合：`Ctrl+Alt+Shift+F9/F10/F11`（用于部分键盘布局或按键映射场景）。
- 热键实现为双通道兜底：  
  `RegisterHotKey + WM_HOTKEY` 主通道  
  `GetAsyncKeyState` 轮询兜底通道（防止个别环境下隐藏态失效）。
- UI 重塑为更统一的 Windows 11 风格浮层界面。
- 顶部与面板升级为渐变玻璃风格，并补充光效层。
- 修复背景渐变细线（分段抗锯齿接缝）导致的“被切割感”。
- 背景进一步简化为轻透明纯色玻璃层，移除分段渐变视觉，降低线条割裂感。
- 背景透明度再提升约 15%，保留文字可读性前提下进一步减轻压迫感。
- 品牌统一：对外名称统一为 `Float Dock`（窗口标题、托盘提示、自启动注册表值、发布产物名）。
- 顶部信息栏收敛为单行标题，避免字体缺失引发杂乱文本。
- 条目与文本对比度增强，避免透明背景导致图标/名称不可读。
- 列表条目默认取消描边，仅在 hover/选中时显示细描边，减少视觉噪声。
- 主面板右键菜单裁剪为极简常用项，保留 Auto-start / Two-column mode / Quit，并统一黑色字体可读性。
- 窗口拖动改为直接拖拽顶部栏（无需按住 Ctrl）。
- 新增窗口边缘/角落拖拽缩放，并在拖动/缩放结束后持久化窗口位置与尺寸；下次启动自动恢复。
- 调整缩放手柄为左右/底部及底角，保留顶部栏用于稳定拖动；拖动时自动限制顶部不移出屏幕。
- 新增双列模式：通过右键菜单 `Two-column mode` 切换，应用列表双列等宽展示。
- 双列长按排序增强：支持跨列拖放；切回单列时按“第一列在前、第二列在后”合并；再次切回双列时恢复原第二列归属（布局记忆）。
- 拖拽固定增强：
  - 允许 `.exe/.lnk/文件夹`
  - 重复固定检测
  - 固定上限 20 条
  - 统一提示反馈
  - 启动时自动迁移历史 `.lnk` 固定项为目标路径（并去重）
  - `.lnk` 解析并保留启动参数与工作目录，避免 `Update.exe + args` 类快捷方式失效
- 删除交互优化：
  - 右键 `Remove` 立即生效并持久化
  - 同步更新选中索引，避免残留状态
- 保留并稳定以下能力：
  - 图标缓存
  - 长按拖拽排序与动画（支持拖到前后位置）
  - 开机自启

## ✅ 验收清单 (Acceptance Checklist)
- [ ] `Ctrl+Alt+Shift+[` 能在隐藏后立即唤起并聚焦
- [ ] `Ctrl+Alt+Shift+]` 只隐藏窗口，不退出进程
- [ ] `Ctrl+Alt+Shift+\` 完全退出程序
- [ ] 中文标题/提示在 Windows 11 默认环境下可正常显示
- [ ] 双击 `.exe` 启动时不弹出终端窗口
- [ ] 拖入非 `.exe/.lnk/文件夹` 给出提示且不入库
- [ ] 拖入文件夹可固定并可双击打开
- [ ] 重复拖入同一程序不会重复固定
- [ ] `.lnk` 条目优先显示目标程序图标而非白纸快捷方式图标
- [ ] 拖入 `.lnk` 后即使删除该快捷方式，固定项仍可正常启动
- [ ] 拖入带参数 `.lnk` 后删除快捷方式，固定项仍按原参数正常启动
- [ ] 右键 `Remove` 后条目立即消失且无需确认弹窗
- [ ] 排序后重启仍保持顺序
- [ ] 长按条目后可拖动到任意前后位置并正确落位
- [ ] 常态界面无明显背景横向细线割裂感
- [ ] `ico/应用名.ico` 能覆盖对应应用图标，未命中时可由 `favicon.ico` 兜底
- [ ] `ico/app.ico` 能生效为 `exe` 文件图标和系统托盘图标
- [ ] 窗口边缘/角落拖拽可实时缩放，交互无卡顿
- [ ] 重启后窗口位置与尺寸与上次退出前一致
- [ ] 顶部栏不会被拖出屏幕外导致无法拖回
- [ ] 右键切换 `Two-column mode` 后列表为双列等宽展示
- [ ] 双列模式下长按排序支持跨列拖放
- [ ] 双列切回单列时，第二列条目会接在第一列末尾
- [ ] 单列再切回双列时，原第二列条目归属可恢复
- [ ] 托盘菜单与右键菜单行为一致可用

---
**版本历史**

| 版本 | 日期 | 作者 | 变更说明 |
| :--- | :--- | :--- | :--- |
| v0.2 | 2026-02-12 | Float Launcher PM | 三热键、图标缓存、布局导入导出与交互规范 |
| v0.3 | 2026-02-12 | Float Launcher PM | 应用栏系统、下滑面板、长按排序与弹性落位 |
| v0.4 | 2026-02-12 | Float Launcher PM | 热键改为 Alt+1/2/3，UI 美化，`app.rs` 模块化重构，拖拽固定规则增强 |
| v0.5 | 2026-02-13 | Float Launcher PM | 原生 RegisterHotKey 提升隐藏态唤起/终止可靠性，加入 Windows 字体回退与渐变玻璃视觉 |
| v0.6 | 2026-02-13 | Float Launcher PM | 热键加轮询兜底，极简无文字顶部，Windows 子系统设置为无终端模式 |
| v0.7 | 2026-02-13 | Float Launcher PM | 热键改为 Ctrl+Alt+Shift+F9/F10/F11，条目更透明更瘦，窗口改高瘦比例 |
| v0.8 | 2026-02-13 | Float Launcher PM | 头部仅保留 Float Dock 单行标题，热键改为 Ctrl+Alt+Shift+[ / ] / \\，并保留 F9/F10/F11 兼容兜底 |
| v0.9 | 2026-02-13 | Float Launcher PM | 提升条目可读性与对比度，主面板右键菜单精简为 Add App/Quick Launch/Auto-start/Quit |
| v1.0 | 2026-02-13 | Float Launcher PM | 支持拖入文件夹，右键菜单仅保留 Auto-start/Quit 并改黑色字体，窗口支持直接拖拽 |
| v1.1 | 2026-02-13 | Float Launcher PM | `.lnk` 图标解析为目标程序图标并缓存重建，移除 Confirm Remove 改为右键即删 |
| v1.2 | 2026-02-13 | Float Launcher PM | `.lnk` 固定项保存为目标路径（非快捷方式路径），删除快捷方式后仍可启动 |
| v1.3 | 2026-02-13 | Float Launcher PM | 启动时自动迁移历史 `.lnk` 固定项到目标路径并去重，旧配置无需手动清理 |
| v1.4 | 2026-02-13 | Float Launcher PM | `.lnk` 解析切换为目标路径 + 启动参数 + 工作目录持久化，修复删除原快捷方式后双击失效 |
| v1.5 | 2026-02-13 | Float Launcher PM | 修复背景渐变细线；长按排序改为稳定计时重绘并优化阈值，支持更可靠前后拖拽 |
| v1.6 | 2026-02-13 | Float Launcher PM | 移除分段渐变改为轻透明纯色背景，条目默认无描边，进一步降低细线割裂与眩晕感 |
| v1.7 | 2026-02-13 | Float Launcher PM | 背景透明度再提升约15%，并新增 `ico` 目录自定义图标覆盖（应用名匹配+favicon兜底） |
| v1.8 | 2026-02-13 | Float Launcher PM | 新增 `ico/app.ico` 构建资源注入，统一 exe 图标与系统托盘图标 |
| v1.9 | 2026-02-13 | Float Dock PM | 品牌统一为 Float Dock：窗口/托盘/自启动名统一，发布产物改为 `float_dock.exe`，保留内部工程名兼容历史配置 |
| v2.0 | 2026-02-13 | Float Dock PM | 新增窗口边缘/角落拖拽缩放与窗口几何持久化（位置+尺寸），启动恢复上次窗口形态 |
| v2.1 | 2026-02-13 | Float Dock PM | 防止标题栏拖出屏幕导致失联；新增右键双列模式（Two-column mode），并将缩放手柄收敛为左右/底部区域 |
| v2.2 | 2026-02-13 | Float Dock PM | 双列长按排序支持跨列拖放；单双列切换加入列归属记忆（单列合并 left+right，回切双列恢复第二列） |






